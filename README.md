# .github
External Secrets operator keeps Kubernetes workloads aligned with real secret stores. External Secrets Kubernetes support syncs Vault, AWS, and GCP secrets into native objects, and External Secrets vault sync removes manual copy steps. Together they give teams a calm, declarative path from store to cluster without secrets in Git.
